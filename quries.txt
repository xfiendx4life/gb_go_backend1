curl -X POST localhost:8000/user/create -H "Content-Type: application/json" -d '{
   "name": "punk",
  "password": "123",
  "email": "punk@mail.ru"
}'

curl -X GET "http://localhost:8000/user/login?name=punk&password=1234"


curl -X POST http://localhost:8000/url -d '{
                "rawurl": "someVeryLongEtsUrl",    
                "userid": 1
          }' -H "Authorization: Bearer <JWT>'
